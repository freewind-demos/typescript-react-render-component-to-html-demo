TypeScript React Render Component to Static HTML Demo
=================================

使用`ReactDOMServer`中的方法可以把一个React组件转换成Html/String

但注意的是，它只能转换静态内容，不能转换state

```
npm install
npm run demo
```

